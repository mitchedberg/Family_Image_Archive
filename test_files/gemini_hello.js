/**
 * Returns a greeting message from Gemini.
 *
 * @param {string} name - The name of the person or entity to greet.
 * @returns {string} A formatted greeting string.
 */
function sayHello(name) {
  return `Hello from Gemini, ${name}!`;
}

console.log(sayHello("World"));
